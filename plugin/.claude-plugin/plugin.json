{
  "name": "winnow",
  "description": "The whole winnow surface as a Claude Code plugin: SessionStart spawns the guard daemon, four more hook events checkpoint agent-team state, five skills prune the transcript on request, and an MCP server exposes diagnosis and treatment. Enabling it starts a background process and lets the model rewrite transcripts; the subset that does neither is what `winnow safe plugin-dir` generates.",
  "author": {
    "name": "the winnow contributors",
    "url": "https://github.com/Xapicc/winnow"
  },
  "repository": "https://github.com/Xapicc/winnow",
  "license": "MIT",
  "keywords": ["context", "pruning", "compaction", "agent-teams", "tokens"]
}
