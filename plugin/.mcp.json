{
  "mcpServers": {
    "winnow": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/..",
        "--extra",
        "mcp",
        "--frozen",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/servers/winnow_mcp.py"
      ]
    }
  }
}
